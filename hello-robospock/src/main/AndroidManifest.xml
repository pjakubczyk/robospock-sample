<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.robospock.sample.simple"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- Robolectric support only API 16 or higher for some reason. Override this value in build.gradle to cover your requirements -->
    <uses-sdk android:minSdkVersion="16" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".SimpleActivity"
            android:label="@string/title_activity_simple_acitivity"
            android:theme="@style/Theme.AppCompat"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>